apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "matstagram.fullname" . }}-test-connection"
  labels:
    {{- include "matstagram.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "matstagram.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
